interface:
  display_name: "BecaWork Logtime"
  short_description: "Set up and manage BecaWork logtime"
  default_prompt: "Use $beca-logtime and its CLI for BecaWork; when authentication is required, rerun the original command in a visible PTY terminal and keep its session attached until completion."
policy:
  allow_implicit_invocation: true
